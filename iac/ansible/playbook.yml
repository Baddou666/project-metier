---
- name: Configure VM with Docker and Git
  hosts: proxmox_vms
  become: true
  gather_facts: false

  vars:
    docker_users:
      - "{{ ansible_user | default('ubuntu') }}"
    project_parent_dir: "/home/{{ ansible_user | default('ubuntu') }}/projet-metier"
    project_repo_url: "https://github.com/Baddou666/project-metier.git"
    project_repo_dir: "{{ project_parent_dir }}/project-metier"
    hf_model_repo_url: "https://huggingface.co/Mitsuman/distilbert"
    hf_model_clone_dir: "{{ project_parent_dir }}/distilbert"
    hf_model_target_dirs:
      - "{{ project_repo_dir }}/backend-services/transformer-model-service/model"
      - "{{ project_repo_dir }}/services-src-code/ai-transformer-model/model"

  pre_tasks:
    - name: Wait for SSH to become available
      wait_for_connection:
        timeout: 120

    - name: Ensure Python is installed for Ansible modules
      raw: test -e /usr/bin/python3 || (apt-get update && apt-get install -y python3)
      changed_when: false

    - name: Gather facts
      setup:

  tasks:
    - name: Update apt repository cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Install base packages
      apt:
        name:
          - ca-certificates
          - curl
          - git
          - git-lfs
          - gnupg
          - lsb-release
          - python3
          - rsync
        state: present

    - name: Create apt keyrings directory
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        keyring: /etc/apt/keyrings/docker.gpg
        state: present

    - name: Add Docker apt repository
      apt_repository:
        repo: "deb [arch={{ ansible_architecture | regex_replace('x86_64', 'amd64') }} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        filename: docker
        state: present
        update_cache: true

    - name: Install Docker packages
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present

    - name: Ensure Docker service is started and enabled
      service:
        name: docker
        state: started
        enabled: true

    - name: Add users to docker group
      user:
        name: "{{ item }}"
        groups: docker
        append: true
      loop: "{{ docker_users }}"

    - name: Create project directory
      file:
        path: "{{ project_parent_dir }}"
        state: directory
        owner: "{{ ansible_user | default('ubuntu') }}"
        group: "{{ ansible_user | default('ubuntu') }}"
        mode: '0755'

    - name: Clone project repository
      git:
        repo: "{{ project_repo_url }}"
        dest: "{{ project_repo_dir }}"
        version: main
        force: true
        update: true
      become_user: "{{ ansible_user | default('ubuntu') }}"

    - name: Configure Git LFS for deployment user
      command:
        argv:
          - git
          - lfs
          - install
          - --skip-repo
      become_user: "{{ ansible_user | default('ubuntu') }}"
      changed_when: false

    - name: Clone Hugging Face model repository
      git:
        repo: "{{ hf_model_repo_url }}"
        dest: "{{ hf_model_clone_dir }}"
        version: main
        update: true
        force: true
      become_user: "{{ ansible_user | default('ubuntu') }}"

    - name: Download Hugging Face Git LFS model files
      command:
        argv:
          - git
          - lfs
          - pull
        chdir: "{{ hf_model_clone_dir }}"
      become_user: "{{ ansible_user | default('ubuntu') }}"

    - name: Ensure model target directories exist
      file:
        path: "{{ item }}"
        state: directory
        owner: "{{ ansible_user | default('ubuntu') }}"
        group: "{{ ansible_user | default('ubuntu') }}"
        mode: '0755'
      loop: "{{ hf_model_target_dirs }}"

    - name: Synchronize downloaded model into project directories
      command:
        argv:
          - rsync
          - -a
          - --delete
          - --exclude=.git
          - "{{ hf_model_clone_dir }}/"
          - "{{ item }}/"
      loop: "{{ hf_model_target_dirs }}"
      become_user: "{{ ansible_user | default('ubuntu') }}"
