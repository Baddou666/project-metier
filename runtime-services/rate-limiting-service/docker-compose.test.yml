#docker compose -f docker-compose.test.yml --env-file .env.test up
include:
  - path: ./docker-compose.yml

services:
  rate-limiting-service:
    image: ghcr.io/baddou666/projet-metier/dev/rate-limiter:test
    ports:
      - 8080:8080
    extra_hosts:
      - "host.docker.internal:host-gateway"
