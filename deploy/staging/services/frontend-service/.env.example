# API_URL: Base URL of your self-managed backend.
# Requests are sent to ${API_URL}/api/ai-detector/detect.
# Leave empty to use the same origin as the frontend.
BACKEND_API_URL=https://ai-detect-staging
FRONTEND_EXT_PORT=3000
FRONTEND_ENV=production
