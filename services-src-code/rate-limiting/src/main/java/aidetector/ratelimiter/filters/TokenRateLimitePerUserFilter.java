package aidetector.ratelimiter.filters;
/*
import aidetector.ratelimiter.model.AnonymousIdentification;
import aidetector.ratelimiter.model.ApiError;
import aidetector.ratelimiter.model.TokenPayload;
import aidetector.ratelimiter.model.TokenRequest;
import aidetector.ratelimiter.services.RateLimitingManager;
import aidetector.ratelimiter.services.TokenJwtManager;
import aidetector.ratelimiter.services.TokenPayloadManager;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.NonNull;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;

@Component
public class TokenRateLimitePerUserFilter extends OncePerRequestFilter {

    private static final Logger logger = LoggerFactory.getLogger(TokenRateLimitePerUserFilter.class);

    RateLimitingManager rateLimitingManager;
    public TokenRateLimitePerUserFilter(RateLimitingManager rateLimitingManager){
        this.rateLimitingManager = rateLimitingManager;
    }
    @Override
    public void doFilterInternal(@NonNull HttpServletRequest req,@NonNull HttpServletResponse res,@NonNull FilterChain chain) throws IOException, ServletException
}
*/